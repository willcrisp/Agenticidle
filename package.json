{
  "name": "agent-idol",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "smoke": "node scripts/smoke.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "balance": "esbuild src/harness/cli.ts --bundle --platform=node --format=esm --outfile=.build/cli.mjs && node .build/cli.mjs",
    "dashboard": "node scripts/build-dashboard.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
